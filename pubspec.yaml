name: velora
description: "AI smart-inventory."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter

  cloud_firestore: ^6.0.3
  firebase_core: ^4.2.1
  cupertino_icons: ^1.0.8
  fl_chart: ^0.68.0
  http: ^1.2.2
  image_picker: ^1.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/